<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema principal da aplicação para o modo noturno -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Estilo para o modo escuro -->
        <item name="android:windowBackground">@color/black</item>
        <item name="android:textColor">@color/white</item>
        <!-- Outros atributos conforme necessário -->
    </style>

    <!-- Tema específico da aplicação -->
    <style name="Theme.Prototipo" parent="AppTheme">
        <!-- Personalizações adicionais para o tema Prototipo -->
    </style>
</resources>
